---
phase: 01-core-simulator
plan: 06
type: execute
wave: 3
depends_on: ["01-04", "01-05"]
files_modified:
  - src/canvas/edges/WireEdge.tsx
  - src/canvas/edges/WireEdge.module.css
  - src/canvas/edges/edgeTypes.ts
  - src/canvas/hooks/useCanvasInteractions.ts
  - src/canvas/hooks/useMagneticSnap.ts
  - src/canvas/hooks/useWireRouting.ts
  - src/canvas/hooks/__tests__/useWireRouting.test.ts
  - src/canvas/hooks/__tests__/useMagneticSnap.test.ts
  - src/canvas/hooks/__tests__/useCanvasInteractions.test.ts
  - src/canvas/Canvas.tsx
autonomous: true
requirements:
  - SCHEM-02
  - SCHEM-03
  - SCHEM-06

must_haves:
  truths:
    - "Wires route at 90-degree angles between component pins"
    - "T-junctions render as filled dots at wire intersection points"
    - "Components can be selected, moved, rotated, copied, and deleted with keyboard shortcuts"
    - "Canvas zooms with scroll wheel and pans with middle-click or space+drag"
    - "Dragging a component near a pin shows magnetic snap feedback and auto-connects"
  artifacts:
    - path: "src/canvas/edges/WireEdge.tsx"
      provides: "Orthogonal wire edge with T-junction dots"
      contains: "BaseEdge"
    - path: "src/canvas/hooks/useCanvasInteractions.ts"
      provides: "Keyboard shortcut handlers for canvas"
      contains: "useHotkeys"
    - path: "src/canvas/hooks/useMagneticSnap.ts"
      provides: "Magnetic snap detection within 20px"
      exports: ["useMagneticSnap"]
  key_links:
    - from: "src/canvas/Canvas.tsx"
      to: "src/canvas/edges/edgeTypes.ts"
      via: "edgeTypes prop on ReactFlow"
      pattern: "edgeTypes"
    - from: "src/canvas/hooks/useCanvasInteractions.ts"
      to: "src/store/circuitStore.ts"
      via: "dispatches circuit actions on keyboard events"
      pattern: "useCircuitStore"
---

<objective>
Implement wire routing with orthogonal edges and T-junctions, canvas interaction handlers (select, move, rotate, copy, delete), magnetic snap, and zoom/pan controls.

Purpose: This makes the schematic editor interactive. Users can connect components with proper EE wires and manipulate the circuit with standard keyboard shortcuts.
Output: Fully interactive canvas with wire routing, selection model, keyboard shortcuts, and magnetic snap behavior.
</objective>

<execution_context>
@.claude/skills/gsd/workflows/execute-plan.md
@.claude/skills/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-simulator/01-CONTEXT.md
@.planning/phases/01-core-simulator/01-UI-SPEC.md
@.planning/phases/01-core-simulator/01-RESEARCH.md

<interfaces>
From src/canvas/Canvas.tsx (Plan 04):
```typescript
// React Flow canvas with nodeTypes, snapGrid, zoom/pan config
```

From src/canvas/components/nodeTypes.ts (Plan 04):
```typescript
export const nodeTypes: NodeTypes = { resistor: ResistorNode, ... };
```

From src/store/circuitStore.ts (Plan 05):
```typescript
export const useCircuitStore: addComponent, removeComponent, updateComponentPosition,
  rotateComponent, addWire, removeWire, clearCircuit, addComponents
// Undo/redo: useCircuitStore.temporal.getState().undo() / .redo()
```

From src/store/uiStore.ts (Plan 05):
```typescript
export const useUiStore: activeTool, setActiveTool, selectedComponentIds, setSelectedComponentIds
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire routing edge and magnetic snap</name>
  <files>
    src/canvas/edges/WireEdge.tsx, src/canvas/edges/WireEdge.module.css,
    src/canvas/edges/edgeTypes.ts, src/canvas/hooks/useMagneticSnap.ts,
    src/canvas/hooks/useWireRouting.ts,
    src/canvas/hooks/__tests__/useWireRouting.test.ts,
    src/canvas/hooks/__tests__/useMagneticSnap.test.ts
  </files>
  <read_first>
    src/canvas/Canvas.tsx (current canvas setup from Plan 04)
    src/canvas/components/ComponentNode.module.css (pin styles for snap targets)
    src/styles/variables.css (wire-stroke, wire-selected, pin-hover, tjunction-dot, accent-primary)
    .planning/phases/01-core-simulator/01-RESEARCH.md (Pattern 2: Orthogonal Wire Edge, smart-edge discussion)
    .planning/phases/01-core-simulator/01-UI-SPEC.md (Wire Routing interactions table, Canvas-Specific Colors for wires)
    .planning/phases/01-core-simulator/01-CONTEXT.md (D-10 through D-14: wire routing decisions)
  </read_first>
  <behavior>
    useWireRouting tests:
    - Test: initial state has isRouting=false, previewPath=null
    - Test: calling startRouting(portId) sets isRouting=true and stores source port
    - Test: calling completeRouting(targetPortId) calls circuitStore.addWire and resets state
    - Test: calling cancelRouting resets isRouting=false and clears preview
    - Test: calling addBend appends a bend point to the preview path

    useMagneticSnap tests:
    - Test: position within 20px of a pin returns snapTarget with port ID and snapped position
    - Test: position more than 20px from any pin returns snapTarget=null
    - Test: isSnapping is true when snapTarget is not null
  </behavior>
  <action>
    Create src/canvas/edges/WireEdge.tsx:
    - Custom React Flow edge that renders orthogonal (90-degree) wire paths per D-10
    - Use getSmoothStepPath from @xyflow/react with borderRadius: 0 for sharp corners
    - Style: stroke var(--wire-stroke) (#4fc3f7), strokeWidth 2
    - Selected state: stroke var(--wire-selected) (#81d4fa)
    - T-junction rendering (D-13): After rendering the edge path, check if any bend point coincides with another wire's path. Render a filled circle (r=5, fill var(--tjunction-dot)) at each T-junction point.
    - For T-junction detection: accept a list of junction points via edge data prop (computed externally and passed in).
    - Wire segments should be individually selectable per D-14 (set interactionWidth to 20 for easier clicking).

    Create src/canvas/edges/WireEdge.module.css:
    - .wireEdge path: stroke var(--wire-stroke), strokeWidth 2
    - .wireEdge.selected path: stroke var(--wire-selected)
    - .tjunctionDot: fill var(--tjunction-dot), r 5px

    Create src/canvas/edges/edgeTypes.ts:
    ```typescript
    import type { EdgeTypes } from '@xyflow/react';
    import { WireEdge } from './WireEdge';

    export const edgeTypes: EdgeTypes = {
      wire: WireEdge,
    };
    ```

    Create src/canvas/hooks/useWireRouting.ts:
    - Hook that manages wire drawing state machine per UI-SPEC Wire Routing interactions:
      1. User clicks pin in wire mode -> starts wire from that pin
      2. Mouse moves -> preview wire follows cursor with orthogonal routing
      3. Click at intermediate point -> fixes a bend point (D-12)
      4. Click on destination pin -> completes wire connection
      5. Escape or right-click -> cancels wire routing
    - Uses uiStore.activeTool to check if wire mode is active
    - Calls circuitStore.addWire on completion
    - Returns: isRouting, previewPath, startRouting, addBend, completeRouting, cancelRouting

    Create src/canvas/hooks/useMagneticSnap.ts:
    - Hook that detects when a dragged component or wire endpoint is within 20px of a valid connection target (pin)
    - Returns: snapTarget (port ID or null), snapPosition, isSnapping
    - Visual feedback: when isSnapping is true, the target pin should glow (6px radius, accent color) per D-01 and UI-SPEC
    - Uses React Flow's getNodes() to find nearby pins

    Write tests in src/canvas/hooks/__tests__/:
    - useWireRouting.test.ts: Use vitest renderHook to test state machine transitions (start, addBend, complete, cancel). Mock circuitStore.
    - useMagneticSnap.test.ts: Test snap threshold detection at 20px boundary. Mock React Flow's getNodes().
  </action>
  <verify>
    <automated>pnpm vitest run src/canvas/hooks/__tests__/useWireRouting.test.ts src/canvas/hooks/__tests__/useMagneticSnap.test.ts --reporter=verbose && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/canvas/edges/WireEdge.tsx imports BaseEdge or getSmoothStepPath from "@xyflow/react"
    - src/canvas/edges/WireEdge.tsx contains "borderRadius: 0" or "borderRadius:0" for sharp corners
    - src/canvas/edges/WireEdge.tsx renders T-junction circles when junction points provided
    - src/canvas/edges/edgeTypes.ts exports edgeTypes with key "wire"
    - src/canvas/hooks/useWireRouting.ts exports useWireRouting hook
    - src/canvas/hooks/useMagneticSnap.ts exports useMagneticSnap with 20px threshold
    - useWireRouting tests pass: state machine transitions verified
    - useMagneticSnap tests pass: 20px threshold verified
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Orthogonal wire edges render with sharp 90-degree corners and T-junction dots. Wire routing state machine handles the full draw-wire flow with tested state transitions. Magnetic snap detects nearby pins within 20px with tested threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Canvas interaction handlers and keyboard shortcuts</name>
  <files>
    src/canvas/hooks/useCanvasInteractions.ts,
    src/canvas/hooks/__tests__/useCanvasInteractions.test.ts,
    src/canvas/Canvas.tsx
  </files>
  <read_first>
    src/canvas/Canvas.tsx (current canvas component)
    src/store/circuitStore.ts (circuit actions: remove, rotate, addComponents)
    src/store/uiStore.ts (activeTool, selectedComponentIds)
    src/canvas/hooks/useWireRouting.ts (created in Task 1)
    src/canvas/hooks/useMagneticSnap.ts (created in Task 1)
    src/canvas/edges/edgeTypes.ts (created in Task 1)
    .planning/phases/01-core-simulator/01-UI-SPEC.md (Interaction Contracts: Canvas Interactions table, Keyboard Shortcuts table)
    .planning/phases/01-core-simulator/01-CONTEXT.md (D-01 through D-05, D-09: interaction decisions)
  </read_first>
  <behavior>
    useCanvasInteractions tests (mock circuitStore and uiStore):
    - Test: Delete key calls removeComponent for each selected component ID
    - Test: R key calls rotateComponent for each selected component ID
    - Test: W key calls setActiveTool('wire')
    - Test: Ctrl+Z calls undo via temporal store
    - Test: Ctrl+Shift+Z calls redo via temporal store
    - Test: Escape calls setActiveTool('select')
  </behavior>
  <action>
    Create src/canvas/hooks/useCanvasInteractions.ts:
    - Uses react-hotkeys-hook for all keyboard shortcuts per UI-SPEC Keyboard Shortcuts table
    - Shortcuts to implement:
      - Delete/Backspace: remove selected components and wires via circuitStore.removeComponent/removeWire
      - R: rotate selected components via circuitStore.rotateComponent (D-09)
      - Ctrl+C: copy selected components to internal clipboard (store in ref)
      - Ctrl+V: paste copied components offset by (20, 20) from original positions, all pasted items become selected (D-05)
      - Ctrl+Z: undo via useCircuitStore.temporal.getState().undo()
      - Ctrl+Shift+Z / Ctrl+Y: redo via useCircuitStore.temporal.getState().redo()
      - Ctrl+A: select all components
      - W: switch to wire tool via uiStore.setActiveTool('wire')
      - V or Escape: switch to select tool
      - Ctrl+K: open command palette (emit event, handled by sidebar search)
      - Ctrl+= / Ctrl+-: zoom in/out via React Flow's zoomIn/zoomOut
      - Ctrl+0: fit view via React Flow's fitView
      - F5: trigger simulation run (emit event or call simulation action)
      - Ctrl+.: cancel simulation
      - ?: toggle keyboard shortcut help overlay
    - Returns the hook's state (clipboard contents count, etc.)

    Write tests in src/canvas/hooks/__tests__/useCanvasInteractions.test.ts:
    - Mock useCircuitStore, useUiStore, and useCircuitStore.temporal
    - Use vitest to verify each hotkey calls the correct store action
    - Test delete, rotate, wire tool, undo, redo, escape shortcuts

    Update src/canvas/Canvas.tsx to integrate:
    1. Import and use edgeTypes from edges/edgeTypes.ts
    2. Import and call useCanvasInteractions hook
    3. Import and call useWireRouting hook
    4. Import and call useMagneticSnap hook
    5. Add onConnect handler that calls circuitStore.addWire
    6. Add onNodesChange handler for position updates (per Research: only update Zustand on drag end, not every frame, to avoid double-render anti-pattern)
    7. Add onDrop and onDragOver handlers for component placement from sidebar (per Research Pattern: Drag-and-Drop from Library to Canvas)
    8. Add wire routing preview as a temporary edge when useWireRouting.isRouting is true
    9. Add snap feedback overlay when useMagneticSnap.isSnapping is true
    10. Connect selectedComponentIds from uiStore to React Flow's selection state

    The onDrop handler must:
    - Read component type from dataTransfer ('application/omnispice-component')
    - Convert screen coordinates to flow position via useReactFlow().screenToFlowPosition
    - Snap to 10px grid (D-02)
    - Call circuitStore.addComponent(type, snappedPosition)
  </action>
  <verify>
    <automated>pnpm vitest run src/canvas/hooks/__tests__/useCanvasInteractions.test.ts --reporter=verbose && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/canvas/hooks/useCanvasInteractions.ts imports "useHotkeys" from "react-hotkeys-hook"
    - src/canvas/hooks/useCanvasInteractions.ts registers handlers for "Delete", "r", "ctrl+c", "ctrl+v", "ctrl+z", "ctrl+shift+z", "w", "f5"
    - src/canvas/Canvas.tsx imports edgeTypes and passes to ReactFlow component
    - src/canvas/Canvas.tsx contains onDrop handler with "screenToFlowPosition" call
    - src/canvas/Canvas.tsx contains onConnect handler
    - src/canvas/Canvas.tsx uses useCanvasInteractions, useWireRouting, and useMagneticSnap hooks
    - useCanvasInteractions tests pass: hotkeys call correct store actions
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Full canvas interaction layer: keyboard shortcuts for all schematic editing operations (with tested hotkey-to-action mapping), wire routing integrated, magnetic snap active, drag-and-drop from sidebar functional, undo/redo wired to Zustand temporal store.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/canvas/hooks/__tests__/ --reporter=verbose` -- all hook tests pass
- `pnpm exec tsc --noEmit` -- zero type errors
- Canvas accepts edgeTypes and nodeTypes
- All keyboard shortcuts registered via react-hotkeys-hook
- Wire routing hook manages full draw-wire lifecycle
- Magnetic snap detects targets within 20px
</verification>

<success_criteria>
Interactive schematic canvas with: orthogonal wire routing (90-degree bends, T-junctions), full keyboard shortcut set (delete, rotate, copy/paste, undo/redo, zoom), magnetic snap on pin proximity, drag-and-drop component placement from sidebar. All per locked decisions D-01 through D-14. All hooks have behavioral tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-simulator/01-06-SUMMARY.md`
</output>
