---
phase: 01-core-simulator
plan: 08
type: execute
wave: 4
depends_on: ["01-04", "01-05", "01-06", "01-07"]
files_modified:
  - src/ui/Toolbar.tsx
  - src/ui/Toolbar.module.css
  - src/ui/Sidebar.tsx
  - src/ui/Sidebar.module.css
  - src/ui/BottomPanel.tsx
  - src/ui/BottomPanel.module.css
  - src/ui/ErrorPanel.tsx
  - src/ui/ErrorPanel.module.css
  - src/ui/PropertyPanel.tsx
  - src/ui/PropertyPanel.module.css
  - src/ui/SimulationControls.tsx
  - src/ui/AnalysisParamsPopover.tsx
  - src/app/Layout.tsx
  - src/app/Layout.module.css
  - src/App.tsx
autonomous: false
requirements:
  - COMP-07
  - COMP-08
  - SCHEM-01

must_haves:
  truths:
    - "User can drag a component from the sidebar library panel onto the canvas"
    - "User can search the component library with fuzzy search and find components by partial name"
    - "User can select analysis type and configure parameters from the toolbar"
    - "User can click Run Simulation and see results in the waveform panel"
    - "Simulation errors display in the error panel with clickable navigation to the problem component"
    - "User can import a .mod/.lib SPICE model file via file dialog"
  artifacts:
    - path: "src/ui/Sidebar.tsx"
      provides: "Component library with categorized list and fuzzy search"
      contains: "cmdk"
    - path: "src/ui/Toolbar.tsx"
      provides: "Toolbar with tool selection, undo/redo, and simulation controls"
      contains: "Run Simulation"
    - path: "src/ui/BottomPanel.tsx"
      provides: "Tabbed bottom panel (Errors, Waveform, Properties)"
      contains: "tab"
    - path: "src/ui/ErrorPanel.tsx"
      provides: "Error list with clickable navigation to canvas"
      contains: "error"
    - path: "src/app/Layout.tsx"
      provides: "Main application layout shell"
      contains: "sidebar"
  key_links:
    - from: "src/ui/Sidebar.tsx"
      to: "src/circuit/componentLibrary.ts"
      via: "reads COMPONENT_LIBRARY for component list"
      pattern: "COMPONENT_LIBRARY"
    - from: "src/ui/Sidebar.tsx"
      to: "src/canvas/Canvas.tsx"
      via: "drag-and-drop with dataTransfer application/omnispice-component"
      pattern: "application/omnispice-component"
    - from: "src/ui/SimulationControls.tsx"
      to: "src/simulation/controller.ts"
      via: "calls runSimulation, cancelSimulation"
      pattern: "SimulationController"
    - from: "src/ui/ErrorPanel.tsx"
      to: "src/store/simulationStore.ts"
      via: "reads errors and validationErrors"
      pattern: "useSimulationStore"
    - from: "src/app/Layout.tsx"
      to: "src/canvas/Canvas.tsx"
      via: "renders canvas in main content area"
      pattern: "Canvas"
---

<objective>
Wire everything together: the application layout shell with sidebar, toolbar, bottom panel, and canvas. Implement the component library sidebar with fuzzy search and drag-to-canvas, simulation controls with analysis type selection and parameter configuration, error panel with clickable navigation, property panel, and SPICE model import.

Purpose: This is the integration plan that turns individual features into a working application. After this plan, the full Phase 1 user journey works: open app, drag components, wire them, configure simulation, run, see results.
Output: A fully functional circuit simulator application.
</objective>

<execution_context>
@.claude/skills/gsd/workflows/execute-plan.md
@.claude/skills/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-simulator/01-CONTEXT.md
@.planning/phases/01-core-simulator/01-UI-SPEC.md
@.planning/phases/01-core-simulator/01-RESEARCH.md

<interfaces>
From src/circuit/componentLibrary.ts (Plan 02):
```typescript
export interface ComponentDefinition { type: ComponentType; name: string; category: string; ... }
export const COMPONENT_LIBRARY: Record<ComponentType, ComponentDefinition>;
```

From src/circuit/validator.ts (Plan 02):
```typescript
export function validateCircuit(circuit: Circuit): ValidationError[];
```

From src/circuit/netlister.ts (Plan 02):
```typescript
export function generateNetlist(circuit: Circuit, config: AnalysisConfig): string;
```

From src/simulation/controller.ts (Plan 03):
```typescript
export class SimulationController { initialize(); runSimulation(netlist, type); cancelSimulation(); loadModel(filename, content); destroy(); }
```

From src/store/circuitStore.ts (Plan 05):
```typescript
export const useCircuitStore: { circuit, addComponent, removeComponent, ... }
```

From src/store/simulationStore.ts (Plan 05):
```typescript
export const useSimulationStore: { status, results, errors, validationErrors, analysisConfig, ... }
```

From src/store/uiStore.ts (Plan 05):
```typescript
export const useUiStore: { activeTool, bottomTab, sidebarCollapsed, bottomPanelCollapsed, ... }
```

From src/canvas/Canvas.tsx (Plan 06):
```typescript
// Fully interactive canvas with node types, edge types, keyboard shortcuts, drag-and-drop
```

From src/waveform/WaveformViewer.tsx (Plan 07):
```typescript
// uPlot waveform viewer with cursor, measurements
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar with component library, search, and drag-to-canvas</name>
  <files>
    src/ui/Sidebar.tsx, src/ui/Sidebar.module.css
  </files>
  <read_first>
    src/circuit/componentLibrary.ts (COMPONENT_LIBRARY data)
    src/store/uiStore.ts (sidebarCollapsed, toggleSidebar)
    src/styles/variables.css (sidebar sizing and colors)
    .planning/phases/01-core-simulator/01-UI-SPEC.md (Sidebar Layout section, Component Library Item States, search bar behavior)
    .planning/phases/01-core-simulator/01-CONTEXT.md (D-29: left sidebar categorized, D-30: fuzzy search, D-31: drag to canvas, D-32: preview icon)
    .planning/phases/01-core-simulator/01-RESEARCH.md (cmdk usage for fuzzy search)
  </read_first>
  <action>
    Create src/ui/Sidebar.tsx:

    Structure per UI-SPEC Sidebar Layout:
    1. Search bar at top (40px tall, 12px horizontal padding) using cmdk for fuzzy matching per D-30
       - Import { Command } from 'cmdk'
       - Command.Input for search field
       - Typing "741" surfaces uA741/LM741
       - Typing "res" surfaces Resistor
       - Clear button (X icon from lucide-react)
    2. Categorized component list per D-29:
       - Categories: "Passives", "Semiconductors", "Sources", "Op-Amps"
       - Each category is a collapsible accordion group (all expanded by default)
       - Category header: 32px tall, font-size var(--font-size-label), font-weight 600
    3. Each component item per D-32:
       - 36px tall row (var(--library-item-height))
       - 20x20 SVG preview icon (miniature version of the EE symbol)
       - Component display name
       - Hover: background var(--surface-hover)
    4. Drag behavior per D-31:
       - onDragStart: set dataTransfer to 'application/omnispice-component' with component type
       - effectAllowed: 'move'
       - Item shows as 50% opacity ghost during drag (per UI-SPEC Component Library Item States)
    5. Collapse behavior:
       - Toggle button at top (ChevronLeft/ChevronRight icon)
       - Collapsed: 40px width, show category icons only, tooltip on hover per UI-SPEC
       - Expanded: var(--sidebar-width) = 240px, min 200px, max 320px
    6. Uses uiStore for collapsed state

    Also handle Ctrl+K globally: when pressed, focus the search input in the sidebar (per UI-SPEC Keyboard Shortcuts).

    Create src/ui/Sidebar.module.css:
    - .sidebar: width var(--sidebar-width), min-width var(--sidebar-min-width), max-width var(--sidebar-max-width), height 100%, background var(--bg-secondary), border-right 1px solid var(--border-default), display flex, flex-direction column, overflow hidden
    - .sidebar.collapsed: width var(--sidebar-collapsed-width), min-width var(--sidebar-collapsed-width)
    - .searchContainer: padding var(--space-sm), height 40px
    - .searchInput: width 100%, height 32px, background var(--surface-primary), border 1px solid var(--border-default), color var(--text-primary), font-size var(--font-size-body), border-radius 4px, padding 0 var(--space-sm)
    - .searchInput:focus: border-color var(--accent-primary), border-width 2px
    - .categoryHeader: height var(--category-header-height), padding 0 var(--space-sm), display flex, align-items center, gap var(--space-xs), font-size var(--font-size-label), font-weight var(--font-weight-semibold), color var(--text-secondary), cursor pointer
    - .componentItem: height var(--library-item-height), padding 0 var(--space-sm), display flex, align-items center, gap var(--space-sm), cursor grab, user-select none
    - .componentItem:hover: background var(--surface-hover)
    - .componentItem.dragging: opacity 0.5
    - .componentIcon: width 20px, height 20px, flex-shrink 0
    - .componentName: font-size var(--font-size-body), color var(--text-primary), white-space nowrap, overflow hidden, text-overflow ellipsis
    - .categoryList: display flex, flex-direction column, overflow-y auto
    - .toggleBtn: width 100%, height 32px, display flex, align-items center, justify-content center, border none, background none, color var(--text-secondary), cursor pointer
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&amp;1 | grep -c "error TS" || echo "0 type errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/Sidebar.tsx imports from "cmdk" for fuzzy search
    - src/ui/Sidebar.tsx imports COMPONENT_LIBRARY from component library
    - src/ui/Sidebar.tsx contains "application/omnispice-component" in drag handler
    - src/ui/Sidebar.tsx renders categories: "Passives", "Semiconductors", "Sources", "Op-Amps"
    - src/ui/Sidebar.module.css contains ".sidebar" with "var(--sidebar-width)" or "--sidebar-width"
    - src/ui/Sidebar.module.css contains ".componentItem" with "var(--library-item-height)" or "36px"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Component library sidebar with categorized list, cmdk fuzzy search, drag-to-canvas, collapse toggle, all styled per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Toolbar, simulation controls, error panel, and app layout shell</name>
  <files>
    src/ui/Toolbar.tsx, src/ui/Toolbar.module.css, src/ui/BottomPanel.tsx,
    src/ui/BottomPanel.module.css, src/ui/ErrorPanel.tsx, src/ui/ErrorPanel.module.css,
    src/ui/PropertyPanel.tsx, src/ui/PropertyPanel.module.css,
    src/ui/SimulationControls.tsx, src/ui/AnalysisParamsPopover.tsx,
    src/app/Layout.tsx, src/app/Layout.module.css, src/App.tsx
  </files>
  <read_first>
    src/ui/Sidebar.tsx (created in Task 1)
    src/store/simulationStore.ts (status, results, errors, analysisConfig)
    src/store/uiStore.ts (activeTool, bottomTab, bottomPanelCollapsed)
    src/store/circuitStore.ts (circuit for validation and netlist generation)
    src/simulation/controller.ts (SimulationController class)
    src/circuit/netlister.ts (generateNetlist function)
    src/circuit/validator.ts (validateCircuit function)
    src/canvas/Canvas.tsx (canvas component to embed in layout)
    src/waveform/WaveformViewer.tsx (waveform component for bottom panel)
    src/waveform/BodePlot.tsx (Bode plot for AC results)
    src/styles/variables.css (all CSS variables)
    .planning/phases/01-core-simulator/01-UI-SPEC.md (Layout section, Toolbar Layout, Bottom Panel Tabs, Button Variants, State Contracts)
    .planning/phases/01-core-simulator/01-CONTEXT.md (D-15 through D-23: simulation and error decisions)
    .planning/phases/01-core-simulator/01-UI-SPEC.md (Copywriting Contract for all UI text)
  </read_first>
  <action>
    Create src/ui/Toolbar.tsx per UI-SPEC Toolbar Layout:
    - Left group: App logo (24x24 icon + "OmniSpice" text), Pointer tool button (icon-only 32x32, ghost variant), Wire tool button (icon-only 32x32, ghost variant)
      - Active tool has background var(--surface-active) per accent reserved list item 3
      - Use MousePointer2 and Minus icons from lucide-react
      - Pointer tooltip: "Select (V)", Wire tooltip: "Draw Wire (W)"
    - Center group: Undo button (RotateCcw icon, tooltip "Undo (Ctrl+Z)"), Redo button (RotateCw icon, tooltip "Redo (Ctrl+Shift+Z)")
      - Undo calls useCircuitStore.temporal.getState().undo()
      - Redo calls useCircuitStore.temporal.getState().redo()
    - Right group: SimulationControls component (analysis dropdown + params + run/cancel)
    - All buttons use Button Variants from UI-SPEC (ghost for tools, primary accent for Run)

    Create src/ui/SimulationControls.tsx:
    - Analysis type dropdown per D-16: "DC Operating Point", "Transient Analysis", "AC Analysis (Bode)", "DC Sweep"
      - Standard select/dropdown, styled per surface-primary background
    - Parameters toggle button: opens AnalysisParamsPopover
    - "Run Simulation" button: primary accent variant (var(--accent-primary) bg, #1a1a2e text)
      - On click: 1) validate circuit (validateCircuit), 2) if errors, set validationErrors and switch to errors tab, 3) if valid, generate netlist (generateNetlist), 4) send to SimulationController.runSimulation
      - During simulation: shows spinner + "Simulating... {elapsed}s" text, button disabled
    - Cancel button: visible only during simulation (D-19), calls SimulationController.cancelSimulation
    - Status per State Contracts table: Loading engine / Simulating / Complete / Error states

    Create src/ui/AnalysisParamsPopover.tsx:
    - Floating popover below toolbar per D-17
    - Shows parameter fields based on selected analysis type:
      - Transient: "Stop Time" (text input, default "10m"), "Time Step" (optional), "Start Time" (optional)
      - AC: "Start Frequency" (default "1"), "Stop Frequency" (default "1MEG"), "Points per Decade" (default 100)
      - DC Sweep: "Source" (dropdown of voltage sources), "Start Value", "Stop Value", "Step Size"
      - DC Op: no parameters
    - Labels per UI-SPEC Copywriting Contract
    - Input fields styled per UI-SPEC Input Field States

    Create src/ui/BottomPanel.tsx per UI-SPEC Bottom Panel Tabs:
    - Tabbed panel with three tabs: Errors, Waveform, Properties
    - Active tab: 2px bottom border var(--accent-primary)
    - Inactive tabs: color var(--text-secondary)
    - Resizable height via drag handle (4px visible, 8px hit area)
    - Collapsible: when collapsed, show 32px tab bar only (tabs still clickable)
    - Tab content:
      - Errors: renders ErrorPanel
      - Waveform: renders WaveformViewer or BodePlot (based on analysisConfig.type)
      - Properties: renders PropertyPanel
    - Auto-switch behavior per State Contracts:
      - Simulation complete: switch to Waveform tab, expand if collapsed
      - Simulation error: switch to Errors tab
      - Component selected: switch to Properties tab

    Create src/ui/ErrorPanel.tsx per D-20 through D-23:
    - List of errors and warnings from simulationStore.errors and simulationStore.validationErrors
    - Each error row shows: severity icon (error=red circle, warning=yellow triangle), message text, suggestion text (dimmer)
    - Click error row: emit event to pan canvas to the component (per D-21: smooth pan animation 200ms ease-out to center the component, red dashed outline on problem component)
    - Empty state: "No issues found." with subtle green checkmark
    - Pre-sim validation warnings show yellow warning icons per D-23

    Create src/ui/PropertyPanel.tsx:
    - Shows when a component is selected
    - Displays: component type, refDesignator, editable value field, rotation, SPICE model info
    - For voltage/current sources: additional fields for AC magnitude, DC offset, frequency, etc.
    - "Import Model" button for COMP-08: opens file dialog accepting .mod, .lib files
      - On file select: read file content, call SimulationController.loadModel(filename, content)

    Create src/app/Layout.tsx as the main shell per UI-SPEC Overall Structure:
    ```
    +--------+------------------------------------------+
    |        |              Toolbar (48px)               |
    |  Side  +------------------------------------------+
    |  bar   |                                           |
    | 240px  |         Canvas (fills remaining)          |
    |        |                                           |
    |        +------------------------------------------+
    |        |         Bottom Panel (280px, collapsible) |
    +--------+------------------------------------------+
    ```
    - CSS Grid or flexbox layout
    - Sidebar on left, toolbar + canvas + bottom panel on right
    - All panels use CSS variables for dimensions
    - Initialize SimulationController in Layout (useRef + useEffect for lifecycle)

    Create src/app/Layout.module.css:
    - .layout: display grid, grid-template-columns var(--sidebar-width) 1fr, grid-template-rows var(--toolbar-height) 1fr auto, height 100vh, width 100vw, background var(--bg-primary)
    - .sidebar: grid-row 1 / -1, grid-column 1
    - .toolbar: grid-row 1, grid-column 2
    - .canvas: grid-row 2, grid-column 2, overflow hidden
    - .bottomPanel: grid-row 3, grid-column 2
    - Handle sidebar collapsed state: grid-template-columns var(--sidebar-collapsed-width) 1fr

    Update src/App.tsx to render Layout instead of placeholder text.

    Wire all copywriting per UI-SPEC Copywriting Contract:
    - Empty state heading: "Start building your circuit"
    - Empty state body: "Drag a component from the library on the left, or press Ctrl+K to search for components."
    - Simulation running: "Simulating... {elapsed}s"
    - Simulation complete: "Simulation complete ({elapsed}s)" toast auto-dismiss 3s
    - Error messages: per TranslatedError content
    - All analysis type labels exactly as specified
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/Toolbar.tsx contains "OmniSpice" text and "Run Simulation" or "Run" button text
    - src/ui/Toolbar.tsx imports undo/redo from circuitStore temporal
    - src/ui/SimulationControls.tsx imports SimulationController
    - src/ui/SimulationControls.tsx calls validateCircuit before simulation
    - src/ui/Sidebar.tsx renders component items with drag handlers
    - src/ui/BottomPanel.tsx renders tabs: "Errors", "Waveform", "Properties"
    - src/ui/ErrorPanel.tsx reads errors from useSimulationStore
    - src/ui/ErrorPanel.tsx contains click handler for navigating to error component
    - src/ui/PropertyPanel.tsx accepts .mod and .lib file uploads
    - src/app/Layout.tsx renders Sidebar, Toolbar, Canvas, and BottomPanel
    - src/App.tsx renders Layout
    - pnpm build succeeds
  </acceptance_criteria>
  <done>Complete application shell with all UI panels wired together. User can: browse/search components, drag to canvas, select analysis type, configure parameters, run simulation, see waveforms, read errors with clickable navigation, edit component properties, import SPICE models.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 1 simulator</name>
  <files>N/A</files>
  <action>Human verifies the complete Phase 1 circuit simulator end-to-end by following the verification steps below.</action>
  <what-built>Complete Phase 1 circuit simulator: schematic editor with IEEE/IEC component symbols, wire routing, component library with fuzzy search, simulation via ngspice WASM worker, waveform viewer with cursors and measurements, error panel with human-readable messages.</what-built>
  <how-to-verify>
    1. Open http://localhost:5173 (run `pnpm dev` if not already running)
    2. Verify dark theme with midnight blue background (#1a1a2e)
    3. Verify sidebar shows categorized component library (Passives, Semiconductors, Sources, Op-Amps)
    4. Type "741" in sidebar search -- verify uA741/LM741 appear
    5. Drag a Resistor, Capacitor, and DC Voltage Source onto canvas
    6. Verify they render as IEEE/IEC symbols (zigzag resistor, parallel plates capacitor, circle with +/- for source)
    7. Click component value label -- verify inline editing works
    8. Press R on selected component -- verify rotation
    9. Use wire tool (W key) to connect components
    10. Verify wires route at 90-degree angles
    11. Add a Ground symbol
    12. Select "Transient Analysis" from toolbar dropdown
    13. Click "Run Simulation"
    14. Verify waveform viewer shows plot (or errors if mock mode)
    15. If waveform shows: click to place cursor, verify readout, try measurements
    16. Test undo/redo (Ctrl+Z / Ctrl+Shift+Z)
    17. Verify error panel shows human-readable messages for any issues
  </how-to-verify>
  <verify>Human visual inspection</verify>
  <done>Phase 1 end-to-end user journey verified: drag components, wire, simulate, view waveforms, read errors.</done>
  <resume-signal>Type "approved" to confirm Phase 1 core functionality works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with zero errors
- `pnpm dev` serves the complete application
- Full user journey: open -> drag components -> wire -> configure sim -> run -> see results
- All UI text matches Copywriting Contract in UI-SPEC
- Dark theme colors match UI-SPEC
- Sidebar search finds components by partial name
</verification>

<success_criteria>
Complete Phase 1 application: component library sidebar with fuzzy search and drag-to-canvas, toolbar with tool selection and simulation controls, interactive schematic canvas with IEEE/IEC symbols and orthogonal wires, waveform viewer with cursors and measurements, error panel with clickable navigation. All wired together and building successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-simulator/01-08-SUMMARY.md`
</output>
