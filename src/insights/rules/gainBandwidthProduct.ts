/**
 * Gain-bandwidth product rule — stub.
 * Task 2 will implement the full AC unity-gain detection.
 */

import type { InsightRule } from '../types';

export const gainBandwidthProduct: InsightRule = {
  id: 'gain-bandwidth-product',
  describe: 'Detects the unity-gain frequency from AC analysis results',
  evaluate: () => null,
};
