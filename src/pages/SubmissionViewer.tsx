// Placeholder — fully implemented in Plan 07

export function SubmissionViewer({ submissionId }: { submissionId: string }) {
  return (
    <div data-testid="submission-placeholder">SubmissionViewer {submissionId} — Plan 07</div>
  );
}
