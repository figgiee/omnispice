/**
 * Plan 05-05 Task 0 — skeleton only. Real assertions land in Task 1 (TDD).
 */
import { describe, it } from 'vitest';

describe.skip('useParameterScrub (Plan 05-05)', () => {
  it('enters scrubbing mode on pointer-down', () => {});
  it('enters sweeping mode on shift + pointer-down when shiftForSweep is true', () => {});
  it('accumulates movementX and calls onChange', () => {});
  it('dispatches omnispice:scrub-committed on pointer-up', () => {});
  it('Escape cancels the scrub and reverts the value', () => {});
  it('onSweepCommit receives final {min,max} range on sweep release', () => {});
});
